Public with sharing class AccountCreater {
    Public Static String createacc(){
        Account acc = new Account();
        acc.Name = 'Test Account';
        insert as user acc;
        return acc.Id;
    }.      =
}